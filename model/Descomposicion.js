class Descomposicion {
    constructor(codigoHijo, factor, rendimiento) {
        this._codigoHijo = codigoHijo
        this._factor = factor
        this._rendimiento = rendimiento
    }
}

module.exports = { Descomposicion }